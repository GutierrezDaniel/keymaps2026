# openspec/config.yaml — keymaps2026
schema: spec-driven

context: |
  Project: keymaps2026 — personal password manager (desktop app).
  Status: greenfield. Repo contains only .gitignore and .atl/ (no source, no manifests).
  Planned stack (user decision, not yet scaffolded): Tauri (Rust core + WebView),
  SQLite storage, React + TypeScript frontend.
  Architecture: hexagonal — Rust domain core, application services, adapters
  (SQLite repository, Tauri command layer).
  Testing: not yet configured; no test runner, linter, type checker, or formatter detected.
  Conventions: conventional commits, no AI attribution in commits, generated artifacts in English.

testing:
  strict_tdd: false
  detected: "2026-08-25"
  status: "Unavailable — greenfield repo has no test runner, manifest, or CI."
  reason: "No runner/config found; sdd-init no-runner fallback sets strict_tdd: false."
  runner: null
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: null
  quality:
    linter: null
    type_checker: null
    formatter: null
  note: "Re-run sdd-init after scaffolding (cargo init / create-tauri-app) to detect real capabilities."

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas